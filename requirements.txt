# Runtime dependencies
PySide6==6.7.2
requests==2.32.3
python-mpv==1.0.7
keyring==25.2.1

# Optional: needed only for scripts/fetch_mpv.py and release builds.
# Install with:  pip install -r requirements-dev.txt
